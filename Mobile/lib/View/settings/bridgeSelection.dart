import 'package:flutter/material.dart';

class BridgeSelection extends StatelessWidget {
  const BridgeSelection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Bridge Selection'),
    );
  }
}
